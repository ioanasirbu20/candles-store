package com.myapp.candles.utils

import com.myapp.candles.dto.OrderDTO
import com.myapp.candles.entities.Order

class OrderMApping {

    fun dtoToEntity(orderDTO: OrderDTO) {
//        return Order(orderDTO.cost, orderDTO.customerId)
    }
}